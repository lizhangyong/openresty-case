
方案1：
     将实例程序的api配置（即:nginx location配置）放到test:Nginx的测试用例中。
     优点： 可以保持test:Nginx原有的特性，不需要与其他api server进行通信交互。
            测试用例不依赖于外部服务。
     缺点： 当实例程序的nginx location发生变化时，测试用例需要跟着变化。
     
方案2:
     test:Nginx测试用例发送请求到api server并获取返回结果。
     优点： 当实例程序的nginx location发生变化时，测试用例不需要变化。
     缺点： 依赖于外部服务，需要处理与外部服务的通信。
     
 
 本实例的测试用例选择方案1. 
